using System;

namespace PZ_04
{
    internal class Program
    {
        static void Main(string[] args)
        {
            string[] catTypes = { "о", "п", "р", "с", "т", "у" };
            foreach (string catType in catTypes)
                Console.WriteLine(catType);

        }
    }
}